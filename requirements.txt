# VoiceType dependencies

# Web framework
# 都加了上界，避免像 httpx 那样某个新大版本悄悄破坏构建（上界高于当前验证过的版本）。
fastapi>=0.104.0,<0.140
uvicorn>=0.24.0,<0.35
python-dotenv>=1.0.0

# LLM client (OpenAI-compatible)
# NOTE: openai<1.55 passes `proxies=` to httpx internally, which httpx>=0.28
# removed. Pin both to the proven-compatible combo so fresh/CI builds don't
# break (older releases worked only because httpx 0.28 didn't exist yet).
openai>=1.30.0,<1.55
httpx>=0.27,<0.28

# Microphone audio capture
sounddevice>=0.4.6,<0.6
numpy>=1.24.0,<2  # NumPy 2.0 会破坏 sherpa-onnx / sounddevice 的 ABI，必须挡住

# Voice recognition (voiceprint/speaker verification - lightweight local solution)
sherpa-onnx>=1.10,<2  # 原本完全没锁版本，native 包，务必锁
pypinyin>=0.55.0  # Required for KWS keyword encoding

# ASR WebSocket client
# 必须 <15：websockets 15 把 extra_headers 改成 additional_headers，会破坏云端 ASR
websockets>=13.0,<15.0

# Keyboard simulation & hotkey
pynput>=1.7.6,<1.8

# Configuration validation
pydantic>=2.0.0,<3

# Process management
psutil>=5.9.0,<8

# System tray
pystray>=0.19.0,<0.20
Pillow>=10.0.0,<12

# Windows: active window detection
pywin32>=306; sys_platform == "win32"

# macOS: active window detection  
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Linux: X11 window detection
python-xlib>=0.33; sys_platform == "linux"

# Audio processing for voiceprint (optional, used by test scripts only)
# scipy>=1.11.0
# librosa==0.10.1

